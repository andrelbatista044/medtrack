document.querySelector('#app').innerHTML=layout('history','Histórico','Consulte os registros da sua rotina.');async function load(){const meds=await API.request('/medications');document.querySelector('#page').innerHTML=`<div class="card"><div class="toolbar"><input type="date" id="start"><input type="date" id="end"><select id="med"><option value="">Todos os medicamentos</option>${meds.map(x=>`<option value="${x.id}">${x.name}</option>`)}</select><select id="status"><option value="">Todos os status</option>${Object.entries(statusNames).map(x=>`<option value="${x[0]}">${x[1]}</option>`)}</select><button class="btn" onclick="records()">Filtrar</button></div><div class="table-wrap"><table><thead><tr><th>Data</th><th>Medicamento</th><th>Previsto</th><th>Registrado</th><th>Status</th></tr></thead><tbody id="rows"></tbody></table></div><div class="notice">O histórico é um registro de organização pessoal e não representa avaliação médica.</div></div>`;records()}async function records(){const q=new URLSearchParams();if(start.value)q.set('start',start.value);if(end.value)q.set('end',end.value);if(med.value)q.set('medication_id',med.value);if(status.value)q.set('status',status.value);const data=await API.request('/history?'+q);rows.innerHTML=data.map(d=>`<tr><td>${new Date(d.scheduled_at).toLocaleDateString('pt-BR')}</td><td><b>${d.medication}</b><br><small>${d.dosage}</small></td><td>${new Date(d.scheduled_at).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</td><td>${d.confirmed_at?new Date(d.confirmed_at).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'}):'—'}</td><td><span class="badge ${d.status}">${statusNames[d.status]}</span></td></tr>`).join('')||'<tr><td colspan="5" class="empty">Nenhum registro encontrado.</td></tr>'}load();

